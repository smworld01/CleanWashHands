<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hands.clean">

    <uses-permission android:name="android.permission.BLUETOOTH" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CleanWashHands">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD_0ExQzqqU-6c-9h9NVBYUpStjC9Tld9c"
            />
        <service
            android:name=".function.service.WashLocationService"
            android:enabled="true"
            android:exported="true" />

        <receiver android:name=".function.receiver.GeofenceBroadcastReceiver" />
        <receiver android:name=".function.receiver.DeviceRegisterReceiver" />
        <receiver android:name=".function.receiver.ServiceStopReceiver" />

        <activity android:name=".activity.settings.TestActivity" />
        <activity
            android:name=".NavigationActivity"
            android:theme="@style/Theme.CleanWashHands.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.TutorialActivity" />
        <activity android:name=".activity.settings.MapsActivity" />
        <activity android:name=".activity.settings.GpsSettingActivity" />
        <activity android:name=".activity.settings.WifiSettingActivity" />
        <activity android:name=".activity.settings.BluetoothSettingActivity" />
    </application>

</manifest>